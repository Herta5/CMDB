# CMDB 项目协作与执行约束

## 适用范围

- 本文件只保留每次工作都必须遵守的执行约束和不可违反的业务红线；详细产品定义以 `docs/project/` 下的项目规范为准。
- 项目规范与本文件不一致、规范之间相互冲突或实现目标超出现有规范时，必须停止相关修改并请求用户确认，不得自行选择解释。

## 项目文档读取规则

- `docs/superpowers/` 仅用于本地设计稿和过程计划，不得加入 Git 索引、提交或推送到远端；本约束覆盖 superpowers 技能对该目录的默认保存与提交要求，禁止使用强制添加绕过 `.gitignore`。
- 涉及产品定位、首期范围或云平台模块边界时，必须先阅读 `docs/project/overview.md`。
- 涉及业务项目、云账号、云资源、访问端点、资源属性或资产状态时，必须先阅读 `docs/project/domain-model.md`。
- 涉及自动同步、手工同步、并发控制、采集结果或资源生命周期流转时，必须先阅读 `docs/project/resource-sync.md`。
- 涉及身份、权限、凭证、敏感信息或审计时，必须先阅读 `docs/project/security.md`。
- 涉及页面、导航、交互、视觉样式或响应式适配时，必须先阅读 `docs/project/frontend-guidelines.md`。
- 一项工作涉及多个领域时，必须阅读全部相关项目规范。

## 不可违反的业务红线

- 项目名称统一为 **CMDB**；CMDB 是面向公有云的资源配置管理平台，业务项目是最高级的数据归属和权限隔离边界。
- 一个云账号或云租户只能归属于一个业务项目，由其同步的资源必须继承同一项目归属。
- 阿里云与 AWS 必须作为同一个 CMDB 服务内边界清晰的独立模块，公共能力不得在平台模块中重复实现。
- 云资源以“接入源 + 资源类型 + 云端唯一 ID”作为唯一身份；云平台原始属性只能由同步模块维护。
- 采集失败、认证失败或任务未执行不得触发资源失联或物理删除；物理删除前必须满足项目同步规范中的失联时长和成功采集确认要求。
- 所有项目级接口必须校验项目成员身份和角色；凭证必须加密存储，密码、令牌、AccessKey、主加密密钥及完整密文不得泄露。

## 技术栈

- 后端使用 Go 1.25.1、Gin 和 GORM。
- 前端使用 Vue 3、TypeScript、Pinia、Vue Router 和 Element Plus。
- 数据库使用 PostgreSQL 17。
- 本地及单机部署使用 Docker Compose。
- 未经用户确认，不得更换上述核心技术栈或引入独立微服务。

## 代码与注释

- 新增或修改代码时，必须根据业务复杂度补充准确、必要的中文注释；简单直观的实现不得为了满足形式要求添加无意义或逐行翻译式注释。
- 新代码文件应说明其职责；结构体、接口、公开函数、核心业务分支和复杂逻辑必须使用中文注释解释业务意图。
- 注释应解释约束、原因和边界条件，不得只把代码逐字翻译成中文。
- 所有用户可见文案、业务错误信息、接口说明和测试用例描述使用中文；业界固定名称、协议名和云产品名可以保留英文。
- 按业务领域组织代码，每个文件只承担清晰、单一的职责；平台模块通过统一接口接入资源核心。
- 禁止复制同一套权限、同步、资产状态或审计逻辑到不同云平台模块。

## 测试与验收

- 新功能和缺陷修复必须先编写能够失败的测试，再编写最小实现使其通过。
- 后端至少运行受影响包测试；跨模块变更在提交前运行 `go test ./...`。
- 前端至少运行相关 Vitest 测试；跨模块变更在提交前运行完整测试、TypeScript 类型检查和生产构建。
- 云平台采集器的自动测试使用接口和模拟响应，不依赖真实云账号。
- 测试必须验证真实业务行为，不得只断言模拟对象被调用。
- 不得把环境权限、依赖下载或已有基线问题伪装成新代码测试通过。

## Git 与工作区安全

- 保留用户已有和未提交的修改，不得覆盖、回退、暂存或提交与当前任务无关的文件。
- 未经用户明确许可，禁止执行 `git reset --hard`、批量删除、强制推送或其他难以恢复的操作。
- 开始任何会修改仓库内容的工作前必须确认当前不在 `main` 分支；若当前位于 `main`，必须先创建并切换到使用 `codex/` 前缀的工作分支，并优先在隔离工作区中执行。
- 所有代码、文档、配置和测试等仓库内容的修改必须在工作分支中完成；除将已验证的工作分支合并回 `main` 所产生的提交外，禁止直接在 `main` 分支创建、修改、删除文件或产生提交。
- 合并前必须确认工作分支已纳入目标 `main` 的最新提交并通过相应验证；执行本地合并时，还必须确认 `main` 工作区干净且不存在未解决冲突。验证失败或任一前提不满足时不得合并。
- 工作完成后必须通过本地合并或 Pull Request 将工作分支合并回 `main`；合并完成后方可视为任务交付完成。
- 每个提交只包含一个可验证的逻辑变更，提交信息应清楚说明目的。
- 删除业务代码前必须确认其能力已被明确取消，或对应能力已经通过测试和验收。
