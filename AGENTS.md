# CMDB 项目协作约束

## 项目定位

- 项目名称统一为 **CMDB**，文档、页面标题、接口说明和部署配置不得使用其他产品名称。
- CMDB 是面向公有云和 Kubernetes 的云资源配置管理平台。
- 业务项目是系统最高级的管理、数据归属和权限隔离边界。
- 一个云账号、云租户或 Kubernetes 集群只能归属于一个业务项目；由其同步的资源继承同一项目归属。
- 产品能力围绕业务项目、云账号、Kubernetes 集群、云资源、同步任务、权限、生命周期和审计展开。

## 首期范围

- 阿里云模块负责 ECS、RDS 和负载均衡。
- AWS 模块负责 EC2、RDS 和 ELB。
- Kubernetes 模块负责集群、Namespace、Node、Workload、Pod、Service 和 Ingress。
- 阿里云、AWS 和 Kubernetes 必须实现为同一个 CMDB 服务内边界清晰的独立模块，不拆分成独立部署服务。
- 公共项目、资源、访问端点、同步任务、权限、生命周期和审计能力不得在平台模块中重复实现。

## 技术栈

- 后端使用 Go 1.25.1、Gin 和 GORM。
- 前端使用 Vue 3、TypeScript、Pinia、Vue Router 和 Element Plus。
- 数据库使用 MySQL 8.4。
- 本地及单机部署使用 Docker Compose。
- 未经用户确认，不得更换上述核心技术栈或引入独立微服务。

## 代码与注释

- 所有新增或修改的代码必须包含准确、必要的中文注释。
- 新代码文件应说明其职责；结构体、接口、公开函数、核心业务分支和复杂逻辑必须使用中文注释解释业务意图。
- 注释应解释约束、原因和边界条件，不得只把代码逐字翻译成中文。
- 所有用户可见文案、业务错误信息、接口说明和测试用例描述使用中文；业界固定名称、协议名和云产品名可以保留英文。
- 按业务领域组织代码，每个文件只承担清晰、单一的职责；平台模块通过统一接口接入资源核心。
- 禁止复制同一套权限、同步、生命周期或审计逻辑到不同云平台模块。

## 数据与生命周期

- 云资源以“接入源 + 资源类型 + 云端唯一 ID”作为唯一身份，不得因重复同步生成重复记录。
- 云平台返回的原始属性由同步模块维护，CMDB 页面和普通业务接口不得手工修改。
- ECS 和 EC2 保存网卡上的内外网 IP；RDS 和负载均衡保存原始域名、端口及最近解析的 IP，不得把动态解析地址当作固定资产属性。
- 云资源首次未被成功同步到时标记为“已失联”；三天内重新出现时恢复原记录并保留审计。
- 资源连续失联满三天后才允许物理删除；采集失败或认证失败不得触发错误的失联判定。
- 物理删除资源时可以同步删除访问端点和资源关系，但审计日志必须保留。

## 安全与权限

- 所有项目级接口必须校验当前用户的项目成员身份和项目角色，禁止跨项目读取或修改数据。
- 云平台凭证必须使用部署环境提供的 `CMDB_ENCRYPTION_KEY` 加密后存储。
- 密码、令牌、AccessKey、kubeconfig、主加密密钥及完整密文不得写入日志、审计内容、接口响应、测试快照或代码仓库。
- API 和页面只显示必要的凭证脱敏信息。
- 无权限访问项目数据时，应避免通过响应泄露目标项目或资源是否存在。

## 同步规则

- 接入源默认每一小时自动同步一次，并支持有权限用户手工触发。
- 同一接入源不得并发运行多个同步任务；不同接入源可以并行。
- 单类资源采集失败时，不得对该类型执行失联判定。
- 部分资源类型失败时，任务标记为“部分成功”，成功类型仍正常入库。
- 接入源认证失败时，不得更新任何资源的失联状态。
- 同步任务必须记录各资源类型的新增、更新、恢复、失联、删除和失败统计，并过滤敏感信息。

## 前端体验

- UI 采用现代云管理控制台风格，使用左侧分组导航和顶部项目切换。
- 阿里云、AWS 和 Kubernetes 分别作为一级功能模块，但共享统一资源核心数据。
- 资源列表优先保证信息密度和可检索性，支持筛选、排序、分页和列配置。
- 页面必须覆盖加载、空数据、失败、无权限和资源失联状态。
- 使用浅色、低饱和的中性色体系，云平台品牌色只用于识别，不使用大面积装饰性渐变。
- 首期适配常见桌面分辨率并保证平板可用，不要求手机端专项适配。

## 测试与验收

- 新功能和缺陷修复必须先编写能够失败的测试，再编写最小实现使其通过。
- 后端至少运行受影响包测试；跨模块变更在提交前运行 `go test ./...`。
- 前端至少运行相关 Vitest 测试；跨模块变更在提交前运行完整测试、TypeScript 类型检查和生产构建。
- 云平台采集器的自动测试使用接口和模拟响应，不依赖真实云账号或真实集群。
- 测试必须验证真实业务行为，不得只断言模拟对象被调用。
- 不得把环境权限、依赖下载或已有基线问题伪装成新代码测试通过。

## Git 与工作区安全

- 保留用户已有和未提交的修改，不得覆盖、回退、暂存或提交与当前任务无关的文件。
- 未经用户明确许可，禁止执行 `git reset --hard`、批量删除、强制推送或其他难以恢复的操作。
- 功能开发使用 `codex/` 前缀的隔离分支，并优先在隔离工作区中执行。
- 每个提交只包含一个可验证的逻辑变更，提交信息应清楚说明目的。
- 删除业务代码前必须确认其能力已被明确取消，或对应能力已经通过测试和验收。
