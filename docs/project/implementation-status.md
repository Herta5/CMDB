# CMDB 实现状态

## 用途与基线

本文以提交 `da0dd19`（完整 SHA：`da0dd199b283c51491a4a9a47252061cd78bdd61`）为本轮实现前 `main` 代码基线，用于追溯当前交付能力和后续差距；它不改变 [项目规范入口](README.md) 及其链接规范中的任何长期要求。状态仅使用“已实现”“部分实现”“待实现”：已实现表示基线及本轮代码和测试已覆盖该能力，部分实现表示已有可用实现但未满足全部规范边界，待实现表示基线未提供该能力。

后续功能提交必须同时更新受影响行的状态、证据和差距。证据均为本轮已纳入工作分支的真实代码或测试路径；测试使用模拟响应或隔离数据库，不连接真实云账号。

本轮在该基线上保留先前的 PostgreSQL 17 空库、接入源身份、全类型负载均衡和外部云 API 加密传输实现链，并新增接入源审计名称快照、按同步任务聚合的资源变化审计及其结构化页面详情，以及按项目汇总正常资产的首页。

## 能力矩阵

当前结构证据：`backend/database/init/001_create_app_role.sh` 创建受限应用账号，`backend/database/init/002_schema.sql` 一次作用于空库并直接创建完整业务结构；两个编号只表示初始化顺序。`backend/internal/platform/database/migration_comments_test.go` 固化首次安装边界，`backend/internal/platform/database/postgres_integration_test.go` 验证首次初始化结构、接入源账号非空与全局唯一、资产限制外键以及应用账号业务读写与 DDL 拒绝。应用镜像只构建 `cmdb-server` 和 `cmdb-init-admin`。

| 能力 | 规范 | 状态 | 基线证据 | 差距 |
| --- | --- | --- | --- | --- |
| 身份认证与会话 | [security.md：身份认证与会话安全](security.md#身份认证与会话安全) | 已实现 | `backend/internal/identity/service.go`、`backend/internal/platform/httpserver/auth.go`、`backend/internal/identity/http_test.go` | 已实现 HS256、24 小时会话、账户代际绑定、实时读取账户状态与统一登录失败提示。 |
| 用户与用户名公开契约 | [domain-model.md：用户](domain-model.md#用户)、[security.md：身份标识暴露边界](security.md#身份标识暴露边界) | 已实现 | `backend/internal/identity/user.go`、`backend/internal/identity/username.go`、`backend/internal/identity/service.go`、`backend/internal/identity/repository.go`、`backend/internal/identity/repository_test.go`、`backend/internal/identity/http_test.go`、`frontend/src/modules/user/` | 用户名校验、唯一性、不可修改、公开 API 转换及用户管理均已实现；PostgreSQL 仅将 `uk_users_username` 约束冲突转换为“用户名已存在”，主键等内部冲突保持服务故障。 |
| 业务项目 | [domain-model.md：业务项目与成员](domain-model.md#业务项目与成员)、[security.md：权限矩阵](security.md#权限矩阵) | 部分实现 | `backend/internal/project/model.go`、`backend/internal/project/service.go`、`backend/internal/api/project_http.go`、`backend/internal/project/service_test.go` | 创建、编辑、启停、删除入口和项目编码约束已有实现；资产依赖删除保护见“项目与接入源删除保护”，停用项目的运行操作拦截见“停用项目运行边界”。 |
| 项目与接入源删除保护 | [domain-model.md：项目与接入源删除保护](domain-model.md#项目与接入源删除保护)、[acceptance.md：删除保护](acceptance.md#ac-028-失联满-24-小时后的成功确认删除) | 已实现 | `backend/internal/resource/deletion_guard.go`、`backend/internal/project/service.go`、`backend/internal/resource/service.go`、`backend/internal/resource/source_service_test.go`、`backend/internal/project/service_test.go`、`backend/internal/platform/database/postgres_integration_test.go`、`backend/database/init/002_schema.sql` | 三类正常或已失联资产及 `queued`、`running` 任务均在事务内阻止父对象删除；先项目、后接入源的统一锁序、稳定 `409`、无成功删除审计和六个 PostgreSQL `ON DELETE RESTRICT` 外键已有业务、并发及真实数据库测试，系统管理员不能绕过。 |
| 项目成员与自我保护 | [domain-model.md：业务项目与成员](domain-model.md#业务项目与成员)、[security.md：自我保护](security.md#自我保护) | 已实现 | `backend/internal/api/project_member_http.go`、`backend/internal/project/service.go`、`backend/internal/project/access_test.go`、`backend/internal/project/service_test.go` | 成员角色、唯一成员关系、负责人不授予权限及管理员自我保护已有实现和测试。 |
| 系统管理员跨项目最高权限 | [overview.md：目标用户与角色](overview.md#目标用户与角色)、[security.md：权限矩阵](security.md#权限矩阵) | 已实现 | `backend/internal/project/access.go`、`backend/internal/project/service.go`、`frontend/src/modules/project/store.ts`、`frontend/src/layouts/ConsoleLayout.vue` | 系统管理员可绕过项目成员关系访问具体项目，并有“所有项目”只读上下文；项目级操作仍使用具体项目路径。 |
| 项目隔离与对象归属 | [domain-model.md：核心对象关系](domain-model.md#核心对象关系)、[security.md：项目隔离与授权执行](security.md#项目隔离与授权执行) | 已实现 | `backend/internal/project/access.go`、`backend/internal/resource/service.go`、`backend/internal/platform/httpserver/server.go`、`backend/internal/project/access_test.go` | 路径项目作为授权边界，接入源和任务均二次核对项目归属，无权与不存在项目统一处理。 |
| 阿里云与 AWS 接入源及模块边界 | [overview.md：首期范围](overview.md#首期范围)、[resource-sync.md：接入源与连接测试](resource-sync.md#接入源与连接测试) | 已实现 | `backend/internal/aliyun/collector.go`、`backend/internal/aliyun/load_balancer.go`、`backend/internal/aliyun/load_balancer_test.go`、`backend/internal/aws/collector.go`、`backend/internal/aws/load_balancer.go`、`backend/internal/aws/load_balancer_test.go`、`backend/internal/resource/collector.go`、`backend/cmd/server/main.go` | 阿里云与 AWS 采集器保持独立模块，经共享资源核心接入；首期范围为阿里云 ECS/RDS/SLB/ALB/NLB/GWLB 与 AWS EC2/RDS/CLB/ALB/NLB/GWLB。 |
| 外部云 API 加密传输 | [security.md：外部传输安全](security.md#外部传输安全)、[resource-sync.md：接入源与连接测试](resource-sync.md#接入源与连接测试)、[acceptance.md：外部云 API 加密传输](acceptance.md#ac-042-外部云-api-加密传输安全场景) | 已实现 | `backend/internal/aliyun/transport.go`、`backend/internal/aliyun/collector.go`、`backend/internal/aliyun/identity.go`、`backend/internal/aliyun/collector_test.go`、`backend/internal/aliyun/identity_test.go`、`backend/internal/aws/collector_test.go`、`backend/internal/aws/identity_test.go` | 阿里云 STS、ECS、RDS、SLB、ALB、NLB、GWLB 统一显式配置 HTTPS，传输层在拨号前拒绝 HTTP 降级；AWS STS、EC2、RDS、Classic Load Balancer、ELBv2 的 SDK v2 HTTPS 默认行为已有签名后进程内回归测试。协议测试只使用虚构凭证和隔离传输，不连接真实云账号；本机回环健康检查和开发代理仍可使用 HTTP。 |
| 接入源凭证、配置与连接测试 | [resource-sync.md：接入源与连接测试](resource-sync.md#接入源与连接测试)、[security.md：凭证加密与更新](security.md#凭证加密与更新) | 已实现 | `backend/internal/resource/credential.go`、`backend/internal/resource/provider.go`、`backend/internal/security/sensitive.go`、`backend/internal/aliyun/identity.go`、`backend/internal/aws/identity.go`、`backend/internal/resource/service.go`、`backend/internal/api/resource.go`、`backend/internal/platform/httpserver/integration_test.go`、`backend/internal/resource/provider_test.go`、`backend/internal/resource/source_service_test.go`、`backend/internal/aliyun/identity_test.go`、`backend/internal/aws/identity_test.go` | AES-256-GCM、两平台凭证对象的完整字段白名单、重复字段与尾随 JSON 拒绝、首期空配置、共享敏感键拦截、联网身份预检和同身份原子替换已有测试；阿里云 STS 显式使用 HTTPS，AWS SDK v2 的 HTTPS 默认行为由进程内出站请求测试保护。创建和替换凭证时，云身份认证、权限和网络失败保持统一安全错误码并返回分类处理建议；Probe 保留独立认证与权限分类。创建和编辑只把有限输入错误映射为 400，内部故障安全返回 500，真实 HTTP 与数据库测试验证来源和成功审计一并回滚；同步与连接读取内部故障也安全返回 500，不存在和跨项目来源保持同一 404。OpenAPI 重构已新增共享原始 JSON 与 Schema 校验，严格拒绝请求外层未知字段、尾随 JSON 和重复凭证键；`backend/internal/platform/httpserver/source_contract_test.go` 验证授权先行、凭证保留语义和失败无副作用。 |
| 云账号或云租户跨项目唯一归属 | [domain-model.md：接入源身份](domain-model.md#接入源身份)、[resource-sync.md：接入源与连接测试](resource-sync.md#接入源与连接测试) | 已实现 | `backend/internal/resource/model.go`、`backend/internal/resource/service.go`、`backend/internal/resource/repository.go`、`backend/internal/resource/source_service_test.go`、`backend/internal/platform/database/postgres_integration_test.go`、`backend/database/init/002_schema.sql` | 新建来源先通过云平台 STS 轻量调用取得稳定账号身份，并原子保存非空账号标识与验证时间；“平台 + 稳定账号标识”普通唯一约束在并发路径提供最终保护。自动测试使用适配器和模拟响应，不连接真实云账号；公开接入源响应按 AC-049 返回只读完整账号标识，验证时间仍隐藏。 |
| PostgreSQL 17 空库首次初始化 | [security.md：数据库首次初始化边界](security.md#数据库首次初始化边界)、[acceptance.md：空库首次初始化](acceptance.md#ac-041-postgresql-17-空库首次初始化安全场景) | 已实现 | `backend/database/init/001_create_app_role.sh`、`backend/database/init/002_schema.sql`、`backend/cmd/init-admin/main.go`、`backend/cmd/init-admin/main_test.go`、`backend/cmd/init-admin/main_postgres_test.go`、`backend/internal/platform/database/database.go`、`backend/internal/platform/database/migration_comments_test.go`、`backend/internal/platform/database/postgres_integration_test.go`、`Dockerfile`、`docker-compose.yml` | Compose 以空数据卷运行 PostgreSQL 17 初始化流程，依次创建受限应用账号和完整业务结构；首次管理员初始化以短事务表锁串行化空库判断，内部主键统一由 identity sequence 分配，真实 PostgreSQL 测试覆盖并发仅成功一次及首个普通用户一次创建成功。应用账号可执行业务 CRUD 但不能执行 DDL；已有数据库或数据卷的结构转换不在支持范围内。 |
| 同步任务、类型级处理与事务 | [resource-sync.md：触发、调度与并发](resource-sync.md#触发调度与并发)、[resource-sync.md：任务状态与结果](resource-sync.md#任务状态与结果)、[resource-sync.md：事务与一致性边界](resource-sync.md#事务与一致性边界) | 已实现 | `backend/internal/resource/sync_decision.go`、`backend/internal/resource/service.go`、`backend/internal/resource/repository.go`、`backend/internal/resource/sync_decision_test.go`、`backend/internal/resource/service_test.go`、`backend/internal/platform/httpserver/integration_test.go` | 任务状态机、逐类型统计和最终事务已实现；预期类型集合必须完整且无重复，零成功类型必为 `failed`。整体、类型集合或最终事务失败不改变资产及最近成功时间；仅非重试自动失败原子推进下次计划，手工和重试失败保持原计划。 手工同步来源读取故障使用安全 500，不存在和跨项目保持统一 404，未读到来源时没有任务或资源副作用。 |
| 同一接入源互斥与异源并行 | [resource-sync.md：触发、调度与并发](resource-sync.md#触发调度与并发) | 已实现 | `backend/internal/resource/service.go`、`backend/internal/resource/service_test.go` | 以接入源锁统一约束手工、自动和重试；不同接入源可并行。 |
| 重试 | [resource-sync.md：重试与服务重启恢复](resource-sync.md#重试与服务重启恢复) | 部分实现 | `backend/internal/resource/service.go`、`backend/internal/api/resource.go`、`backend/internal/resource/service_test.go`、`backend/internal/resource/source_service_test.go` | 失败/部分成功任务会新建关联原任务的 `queued`、`manual` 重试；停用项目仍未被统一拦截新的重试。 |
| 服务重启恢复 | [resource-sync.md：重试与服务重启恢复](resource-sync.md#重试与服务重启恢复) | 部分实现 | `backend/internal/resource/service.go`、`backend/internal/resource/repository.go`、`backend/internal/resource/source_service_test.go`、`backend/cmd/server/main.go`、`backend/cmd/server/main_test.go` | 已恢复 `queued` 任务并将遗留 `running` 收敛为安全失败；恢复事务失败会阻止服务启动。基线仍未为一般中断收敛写入“系统任务”审计，且停用项目未纳入新的运行边界统一校验。 |
| 停用项目运行边界 | [security.md：权限矩阵](security.md#权限矩阵)、[frontend-guidelines.md：信息架构](frontend-guidelines.md#信息架构) | 待实现 | `backend/internal/platform/httpserver/server.go`、`backend/internal/api/resource.go`、`backend/internal/resource/service.go`、`frontend/src/modules/resource/store.ts` | 基线路由与服务未统一在项目停用时阻止新的连接测试、手工同步、重试和自动任务创建；前端也未据项目状态统一禁用这些操作。已受理任务继续运行的既有语义不受此差距影响。 |
| 三类资产模型与统一只读视图 | [domain-model.md：云资源模型](domain-model.md#云资源模型)、[overview.md：首期范围](overview.md#首期范围) | 已实现 | `backend/internal/resource/model.go`、`backend/internal/resource/asset_storage.go`、`backend/internal/resource/asset_storage_test.go`、`backend/internal/resource/service.go`、`backend/internal/resource/service_test.go` | ECS/EC2、两家 RDS、SLB/CLB/ALB/NLB/GWLB 分别写入三类模型，并由资源核心统一查询；负载均衡五种具体类型共用资产表，`provider` 区分同名类型。 |
| 全类型负载均衡采集与展示 | [resource-sync.md：负载均衡产品采集](resource-sync.md#负载均衡产品采集)、[frontend-guidelines.md：页面职责](frontend-guidelines.md#页面职责) | 已实现 | `backend/internal/aliyun/load_balancer.go`、`backend/internal/aliyun/load_balancer_test.go`、`backend/internal/aliyun/collector_test.go`、`backend/internal/aws/load_balancer.go`、`backend/internal/aws/load_balancer_test.go`、`backend/internal/aws/collector_test.go`、`backend/internal/resource/asset_storage_test.go`、`backend/internal/resource/service_test.go`、`frontend/src/modules/resource/AssetListPage.vue`、`frontend/src/modules/resource/CloudPlatformPage.vue`、`frontend/src/modules/project/pages.test.ts`、`frontend/src/modules/audit/AuditLogPage.vue`、`frontend/src/modules/audit/page.test.ts` | 完整同步在单个接入源 Region 内全分页；阿里云四个产品独立隔离，AWS Classic 独立采集、ELBv2 只读取一次后按 ALB/NLB/GWLB 分组。单类监听器失败不推进该类生命周期，ELBv2 列表失败或未知类型使三类安全失败；Probe 仅调用最小列表页面。负载均衡页查询 `slb`、`clb`、`alb`、`nlb`、`gwlb`，平台页分别列出各自完整范围，审计使用官方大写缩写，不再产生旧 `elb`。 |
| 服务器计算规格与云盘明细 | [domain-model.md：类型专属字段](domain-model.md#类型专属字段)、[resource-sync.md：类型级处理与统计](resource-sync.md#类型级处理与统计)、[frontend-guidelines.md：页面职责](frontend-guidelines.md#页面职责) | 已实现 | `backend/internal/aliyun/collector.go`、`backend/internal/aliyun/collector_test.go`、`backend/internal/aws/collector.go`、`backend/internal/aws/collector_test.go`、`backend/internal/resource/collector.go`、`backend/internal/resource/collector_test.go`、`backend/internal/resource/model.go`、`backend/internal/resource/asset_storage.go`、`backend/internal/resource/asset_storage_test.go`、`backend/internal/resource/service_test.go`、`backend/database/init/002_schema.sql`、`frontend/src/modules/resource/api.ts`、`frontend/src/modules/resource/AssetListPage.vue`、`frontend/src/modules/project/pages.test.ts` | ECS/EC2 统一保存 `instance_type`、`vcpu`、以 MiB 为单位的 `memory` 和已挂载云盘 `disks`；阿里云只接收 `In_use` 云盘并从原始响应确认加密状态存在，AWS 只接收 `attached` EBS 并要求存在根设备名，磁盘按稳定 ID 排序去重并区分系统盘/数据盘，不包含无稳定云盘 ID 的临时本地盘。补充 API、规格或已挂载磁盘必要字段不完整时整类失败且保留旧快照；单类型权限或采集错误不阻断其他类型，凭证认证失效整体终止。连接测试覆盖新增只读权限。服务器列表只展示磁盘总容量，右侧只读详情抽屉展示完整磁盘明细（无云盘时为 `[]`）；公开资源响应不返回原始属性快照。 |
| 数据库计算规格与存储容量 | [domain-model.md：类型专属字段](domain-model.md#类型专属字段)、[resource-sync.md：类型级处理与统计](resource-sync.md#类型级处理与统计)、[frontend-guidelines.md：页面职责](frontend-guidelines.md#页面职责) | 已实现 | `backend/internal/aliyun/collector.go`、`backend/internal/aliyun/collector_test.go`、`backend/internal/aws/collector.go`、`backend/internal/aws/collector_test.go`、`backend/internal/resource/collector.go`、`backend/internal/resource/model.go`、`backend/internal/resource/asset_storage.go`、`backend/internal/resource/asset_storage_test.go`、`backend/internal/resource/service_test.go`、`backend/internal/platform/database/migration_comments_test.go`、`backend/database/init/002_schema.sql`、`frontend/src/modules/resource/api.ts`、`frontend/src/modules/resource/AssetListPage.vue`、`frontend/src/modules/project/pages.test.ts` | 阿里云与 AWS RDS 统一保存 `instance_type`、可空的 `vcpu` 与 MiB `memory`、`storage_type` 和可空的 GiB `storage_size_gib`。阿里云逐实例读取详情，AWS 通过去除 `db.` 前缀后的 EC2 类型补充固定规格；Serverless 或无固定类型时 CPU、内存为空。补充 API 失败或阿里云详情缺失时 RDS 类型失败并保留旧快照。数据库列表将实例类型、vCPU、内存、引擎、版本和存储容量分列展示，不构造逐盘明细；无法确认的展示值统一为 `—`，存储类型仅在详情显示。 |
| 资源身份与生命周期 | [domain-model.md：状态与时间语义](domain-model.md#状态与时间语义)、[resource-sync.md：资源生命周期](resource-sync.md#资源生命周期) | 部分实现 | `backend/internal/resource/model.go`、`backend/internal/resource/asset_storage.go`、`backend/internal/resource/service.go`、`backend/internal/resource/service_test.go` | 资源唯一身份与按类型同步生命周期已有实现，成功类型的变更在同一最终事务提交；端点比较包含 `ResolvedIPs`，仅 DNS 解析 IP 变化也会增加更新统计、刷新业务更新时间并计入该任务唯一 `source.synced` 的 `updated` 分组，未满足 AC-025。 |
| 审计与敏感信息 | [security.md：审计动作与一致性](security.md#审计动作与一致性)、[security.md：审计查询分页与保留](security.md#审计查询分页与保留) | 部分实现 | `backend/internal/audit/context.go`、`backend/internal/audit/repository.go`、`backend/internal/api/audit.go`、`backend/internal/audit/repository_test.go`、`backend/internal/resource/sync_audit.go`、`backend/internal/resource/service_test.go`、`frontend/src/modules/audit/AuditLogPage.vue`、`frontend/src/modules/audit/sync-detail.ts`、`frontend/src/modules/audit/sync-detail.test.ts`、`frontend/src/modules/audit/page.test.ts`、`frontend/src/styles/base.css` | 重要动作审计、事务绑定、权限查询、稳定分页和已分类敏感键递归过滤已实现；每个同步任务只追加一条 `source.synced`，以动作、资源类型和云端唯一 ID 聚合追溯真实变化，接入源名称以操作时快照展示。列表以文字和图形线索直接展示普通管理、同步与连接测试结果，未知历史状态不会被误判为成功；操作人以显示名称在上、用户名在下展示，同步详情仍保持结构化。共享分类采用敏感键匹配，不猜测未知语义，也没有审计字段允许列表。新增或重命名敏感字段须在发布前更新共享分类并补回归测试；重启中断任务的系统审计仍未补齐。 |
| 统一云同步管理入口 | [overview.md：首期范围](overview.md#首期范围)、[frontend-guidelines.md：信息架构](frontend-guidelines.md#信息架构) | 已实现 | `frontend/src/router/index.ts`、`frontend/src/layouts/ConsoleLayout.vue`、`frontend/src/modules/resource/CloudSyncManagementPage.vue`、`frontend/src/modules/resource/CloudPlatformPage.vue`、`frontend/src/modules/resource/store.ts`、`frontend/src/modules/resource/store.test.ts`、`frontend/src/app-flow.test.ts` | 控制台统一汇总阿里云与 AWS 来源和任务，提供接入源创建、编辑、启停、连接测试、同步与删除。真实状态层和组件测试覆盖创建/编辑后的事实刷新、项目切换隔离、弹窗迟到回调、同项目轮询及双提交反序完成，凭证输入在各结束路径清理。 |
| 首页正常资产概览 | [frontend-guidelines.md：信息架构](frontend-guidelines.md#信息架构)、[frontend-guidelines.md：页面职责](frontend-guidelines.md#页面职责)、[acceptance.md：导航、页面职责与项目切换隔离](acceptance.md#ac-033-导航页面职责与项目切换隔离) | 已实现 | `frontend/src/router/index.ts`、`frontend/src/layouts/ConsoleLayout.vue`、`frontend/src/modules/home/HomePage.vue`、`frontend/src/modules/project/pages.test.ts`、`frontend/src/router/index.test.ts` | 登录后默认进入首页；首页按具体项目或系统管理员“所有项目”上下文，使用服务端分页总数汇总正常服务器、数据库和负载均衡，不包含已失联资产，并提供三类资产列表入口。请求版本隔离阻止旧项目统计覆盖新上下文。 |
| 资源列表筛选 | [frontend-guidelines.md：资源列表](frontend-guidelines.md#资源列表) | 已实现 | `backend/internal/api/resource.go`、`backend/internal/resource/service.go`、`backend/internal/resource/repository.go`、`backend/internal/resource/service_test.go`、`backend/internal/api/resource_query_test.go`、`backend/internal/platform/httpserver/integration_test.go`、`frontend/src/modules/resource/AssetListPage.vue`、`frontend/src/modules/project/pages.test.ts` | 三类页面均将搜索、筛选、列设置和刷新列表按顺序集中在列表卡片顶部。三类均支持名称和云端唯一 ID 的全量模糊搜索；服务器与数据库另支持实例类型，服务器另支持内外网 IP，数据库与负载均衡另支持完整访问地址。服务器提供平台、完整接入源、类型、地域、云端状态、资产状态筛选；数据库以大小写不敏感精确值引擎筛选替代类型筛选；负载均衡提供平台、完整接入源、类型、归一化网络类型、地域、云端状态和资产状态筛选。网络类型和云端状态筛选均覆盖两平台同一页面语义下的原始值。服务端先匹配再分页，并提供仅系统管理员可用的“所有项目”统一查询。 |
| 资源列表必显字段 | [frontend-guidelines.md：资源列表](frontend-guidelines.md#资源列表) | 已实现 | `backend/internal/resource/model.go`、`backend/internal/resource/repository.go`、`frontend/src/modules/resource/AssetListPage.vue`、`frontend/src/modules/resource/api.ts`、`frontend/src/modules/project/pages.test.ts` | 三类页面均按确认列结构展示核心字段，无法确认的字段显示 `—`，“所有项目”额外必显项目，名称可打开不含原始属性的右侧只读详情。服务器展示分列规格、磁盘总量与内外网 IP；数据库展示分列规格、引擎、存储容量和完整访问地址；负载均衡展示来源、官方大写类型、网络类型、地域、完整访问地址、双状态和最近发现时间。列表地域不混入可用区，数据库与负载均衡列表不显示协议或解析 IP。 |
| 资源列表分页 | [frontend-guidelines.md：资源列表](frontend-guidelines.md#资源列表) | 已实现 | `backend/internal/platform/httpserver/server.go`、`backend/internal/api/resource.go`、`backend/internal/resource/service.go`、`backend/internal/resource/repository.go`、`backend/internal/resource/service_test.go`、`frontend/src/modules/resource/api.ts`、`frontend/src/modules/resource/AssetListPage.vue`、`frontend/src/modules/project/pages.test.ts` | 三类页面在单项目及系统管理员“所有项目”上下文均使用服务端统一分页，默认 20 条并可选 20/50/100，页面显示当前页和总数。负载均衡五类资源通过一次统一查询，由数据库在完整地址搜索和计数后按公开字段投影、稳定排序并执行 `LIMIT/OFFSET`；不读取原始属性或接入源凭证，也不再逐类型定量加载后在浏览器汇总。 |
| 资源列表排序 | [frontend-guidelines.md：资源列表](frontend-guidelines.md#资源列表) | 已实现 | `backend/internal/resource/repository.go`、`backend/internal/resource/service_test.go`、`frontend/src/modules/resource/AssetListPage.vue`、`frontend/src/modules/project/pages.test.ts` | 资源列表不提供前端排序切换；三类页面所有请求固定按资源名称升序，表头和列表标题不显示排序按钮、箭头或说明，后端使用“接入源 + 类型 + 云端唯一 ID”作为稳定次级排序。 |
| 资源列表列配置 | [frontend-guidelines.md：资源列表](frontend-guidelines.md#资源列表) | 已实现 | `frontend/src/modules/resource/AssetListPage.vue`、`frontend/src/modules/project/pages.test.ts` | 服务器页可隐藏来源、类型、实例类型和地域；数据库页可隐藏来源、类型、实例类型、引擎版本和地域；负载均衡页可隐藏来源、类型、网络类型和地域。三类均可调整列顺序、恢复默认并按用户名、页面类型和项目上下文持久化，确认的核心列均不可隐藏。 |
| 控制台页面、状态与项目切换 | [frontend-guidelines.md：页面职责](frontend-guidelines.md#页面职责)、[frontend-guidelines.md：管理操作与页面状态](frontend-guidelines.md#管理操作与页面状态) | 部分实现 | `frontend/src/router/index.ts`、`frontend/src/layouts/ConsoleLayout.vue`、`frontend/src/styles/base.css`、`frontend/src/modules/project/store.ts`、`frontend/src/modules/project/pages.test.ts`、`frontend/src/session-races.test.ts`、`frontend/src/app-flow.test.ts` | 已有中文导航、统一的首页与功能入口默认/选中样式、角色路由边界、项目上下文、请求竞态清理及主要加载/空/失败状态；普通项目成员仍被导航与路由限制进入项目列表和云同步管理，尚不满足这两页的项目内只读可达要求，停用项目操作限制也尚未统一。 |

## 后续更新规则

实现任何“部分实现”或“待实现”项时，必须先满足对应规范和验收场景，再在本文件更新状态、具体证据路径及剩余差距；不得仅因页面入口、模拟调用或单一单元测试存在就改变状态。

## 运行可靠性增量（基线 85cfe2a）

| 能力 | 状态 | 实现与验收证据 | 边界与剩余差距 |
| --- | --- | --- | --- |
| 固定 PostgreSQL 集成验证 | 已实现 | `scripts/test-backend.sh`、`.github/workflows/backend.yml`、`backend/test/postgres/docker-compose.yml`、`backend/internal/platform/database/backend_test_entrypoint_test.go`、两份 postgres 标签测试；AC-043 | 修复测试 Compose 被忽略导致干净检出失败的问题；本地入口已实际运行 PostgreSQL 17 与全部后端测试并清理环境。远端 GitHub Actions 要在推送后运行，本次不宣称远端已通过。 |
| 同步并发、超时与退出 | 已实现 | `backend/internal/resource/execution.go`、`execution_test.go`、`service.go`、`repository.go`、`backend/internal/aliyun/transport_test.go`、`backend/cmd/server/lifecycle.go`；AC-044 | 默认并发 4、运行 900 秒、独立失败收敛 10 秒；前置读写超时、持久化活动互斥、资产保留、排队预算和恢复限额已有回归；仅支持单应用实例，外部强杀或数据库持续不可用仍需重启恢复。 |
| 安全诊断与连接池预算 | 已实现 | `backend/internal/platform/diagnostics/`、`httpserver/diagnostics_test.go`、`resource/diagnostics_test.go`、`database/pool_test.go`、`config/config_test.go`；AC-045 | HTTP、同步与数据库使用安全事件与关联，异常不输出原文；默认连接池 20/5/1800 秒。没有新增外部日志收集平台、指标监控或日志留存服务。 |

## OpenAPI 契约重构交付

本次重构以 `2a48065` 为实现前基线，保留 Go 1.25.1、Gin、GORM、Vue 与 PostgreSQL 17。公开 HTTP 适配从领域包迁入 `backend/internal/api/`，通过生成的严格服务端接口调用领域服务；领域服务和仓储不依赖生成类型。

| 能力 | 状态 | 实现与验证证据 | 边界 |
| --- | --- | --- | --- |
| 全量公开接口契约 | 已实现 | `api/openapi.yaml`、`api/schemas/`、`backend/internal/api/generated/contract_test.go` | 覆盖 30 个操作；引用、操作集合、凭证语义和原始 operationId 保留检查通过，双端生成结果一致。 |
| Go 生成接口与授权 | 已实现 | `backend/internal/api/`、`backend/internal/platform/httpserver/contract_test.go`、`source_contract_test.go`、`binding_contract_test.go` | 生成路由为唯一生产入口；真实成功与失败响应、全部匿名操作拒绝、输入严格解析和安全错误兜底通过回归，完整 PostgreSQL 17 后端验收通过。 |
| 前端生成调用与会话 | 已实现 | `frontend/orval.config.ts`、`frontend/src/api/`、`frontend/src/modules/*/api.ts` | 全部业务模块使用生成调用，保持显示模型及共享会话保护；15 个测试文件、195 个测试、类型检查与生产构建通过。 |
| 生成与兼容门禁 | 已实现 | `scripts/generate-api.sh`、`scripts/check-api.sh`、`.github/workflows/api.yml` | 固定版本；统一生成与历史契约兼容入口通过，删除操作负向测试按预期拒绝；首次引入明确建立基线。CI 配置已纳入，本地执行了相同入口，未声明远端工作流已运行。 |

类型非法的数字查询现在返回安全 `400`，不再将非数字字符串静默当作零值。合法请求的路径、公开字段、默认值、安全业务错误、项目隔离和资源生命周期沿用既有语义。离线文档提供 bundled OpenAPI，未增加生产在线调试入口或运行时响应 Schema 校验。

本轮生产 Docker 镜像已构建通过；验收环境使用隔离 PostgreSQL 容器且已清理，未访问真实云账号，未变更正在运行的部署。前端构建保留既有依赖注释及较大 chunk 提示，不属于本次接口契约重构的功能差距。

## 资产列表空筛选回归修复

以 `675b568` 为修复前基线，资产页未填写的筛选被序列化为 `provider=` 等空参数，在 OpenAPI 请求校验中返回 `400 RESOURCE_INVALID_QUERY`。`frontend/src/modules/resource/api.ts` 现对具体项目及所有项目查询统一省略空字符串，保留有效筛选、数字零、排序与分页，不修改调用方对象或放宽服务端校验；对应要求见 [frontend-guidelines.md](frontend-guidelines.md#资源列表)，验收见 AC-048。

已实现并验证：`frontend/src/modules/resource/api.test.ts` 新增 8 项真实 Axios URL 序列化回归，其中 6 项在修复前因空筛选失败、修复后通过；`frontend/src/modules/project/pages.test.ts` 验证移除筛选后不再传空值。前端全量 203 项测试与类型检查通过，生产 Docker 镜像构建通过；隔离 PostgreSQL 17 与真实应用中，三类资产在两类项目上下文的请求均返回 `200`，原始空参数请求均复现 `400`。隔离验证仅使用虚构账号和空资产数据，未连接现有业务数据库或真实云平台；测试容器与网络已清理。


## 接入源卡片云账号 ID 展示

以 `a3dcfa6` 为实现前基线，按 AC-049 在接入源名称下只读展示完整 AWS Account ID 与阿里云账号 UID；使用字符串保留前导零，窄卡片允许换行。`api/schemas/sources.yaml` 声明只读 `cloud_account_id`，双端类型由现有工具生成；`backend/internal/api/dto.go` 仅显式增加账号字段，领域模型直接 JSON 输出仍隐藏身份字段，不涉及数据库结构变更。

实现证据：`backend/internal/platform/httpserver/integration_test.go` 覆盖创建与普通编辑响应，`source_contract_test.go` 覆盖两平台完整账号、当前角色与成员关系、匿名和无权读取、客户端指定账号被拒绝；`frontend/src/api/client-contract.test.ts` 验证字段映射与编辑请求，`frontend/src/modules/project/pages.test.ts` 验证卡片、编辑表单隔离和项目切换清理。前端全量 205 项测试、类型检查和生产构建通过；`scripts/test-backend.sh` 使用隔离 PostgreSQL 17 运行全量后端测试通过，并完成测试容器与网络清理；`scripts/check-api.sh main` 的生成一致性与历史接口兼容检查通过，生产 Docker 镜像已构建。

边界：仅授权接入源响应和卡片展示账号，凭证、密文与身份验证时间继续隐藏，错误、运行日志和审计不增加账号标识。项目成员的 HTTP 读取权限已验证；上表已记录的普通成员导航与路由只读可达差距仍存在，此次没有修改页面准入或操作权限。云身份测试使用虚构账号与模拟适配器。

## 用户管理展示与编辑内删除

以 `c6363ff` 为本轮实现前基线，按 AC-050 调整用户管理：显示名称在上、用户名在下，缺失显示名称时回退为用户名；系统管理员的项目权限显示“所有项目”，普通用户显示授权项目数量或“未授权”。列表只保留编辑，删除用户入口移至编辑弹窗底部左侧，保留原始用户身份确认、当前管理员自我保护、取消后返回编辑、删除等待与失败重试；成功后关闭两个弹窗，密码在进入删除确认和关闭时清理。

实现证据为 `frontend/src/modules/user/UserManagementPage.vue` 与 `frontend/src/modules/project/pages.test.ts`：使用真实 Vue 渲染与业务状态验证显示顺序、有效权限、删除入口、取消、延迟成功、失败重试及自身保护。前端全量 210 项测试、TypeScript 类型检查和生产 Docker 镜像构建通过。本轮只修改前端交互与正式规范，后端授权和用户名 API 契约沿用现有实现，没有变更数据库结构。


以 `2b4079c` 为基线补齐编辑弹窗的全局角色展示：系统管理员隐藏逐项目授权控件及普通用户角色说明，仅提示拥有所有项目权限；普通用户仍可逐项目授权。仅改变展示，不清空原有成员关系或表单选择。`frontend/src/modules/project/pages.test.ts` 新增创建和编辑的角色切换回归，验证管理员控件隐藏及切回普通用户后的项目、角色恢复；前端全量 212 项测试和生产 Docker 镜像构建通过，AC-050 与前端规范已同步。
